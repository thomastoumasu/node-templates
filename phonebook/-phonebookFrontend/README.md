depending on baseURL in phonebook.js:

a)
npm run dev and run backend with
npm run server on other terminal (json server) / ok
or with PhonebookFullstack (there use cors)

b)
or can be used as automatically generated frontend from PhonebookFullstack (intial data in variable) or phonebookDatabase (initial data in mongodb) / ok
