Default configuration:  
First npm install in -phonebookDatabase and in -phonebookFrontend
in -phonebookDatabase, npm install then build the frontend with npm run build:ui. Then run the app from there with npm start.  
If the backend is not connected, check baseURL in -phonebookFrontend/src/services/phonebook.js.

check the readme's for additional options.
