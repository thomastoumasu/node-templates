# Templates for Node apps

## Fullstack apps

- BlogList (with frontend, backend and e2e tests, router, states in redux and states in query/context)
- phonebook (backend also serves frontend, containerized and run serverless on GCR)

## Fetch with axios

- currency: minimal fetch of one api
- countries: two apis with more React features

## minimal React frontend setup with json server

- minFrontEnd: just fetch and display db data as list
- minFrontend_unicafe: simple Buttons

## minimal express backend with db

- minBackend: in one file, to start from scratch
- stable CRUD backend structure with middleware and tests
