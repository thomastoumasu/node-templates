from https://fullstackopen.com/en/part5/

npm run dev to run this frontend / ok
backend: (both served on http://localhost:3003/api/blogs)
npm run server here on other terminal (json server, uses routes option and routes.json to serve on api/) / ok
npm run dev in blogListBackend (there app.use(cors())) - this uses mongodb connexion. / ok

npm run lint / ok
npm run test / ok
